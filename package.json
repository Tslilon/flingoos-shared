{
  "name": "@flingoos/shared",
  "version": "0.2.0",
  "description": "Shared types, schemas, and validation for Flingoos services - NO business logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./types": "./dist/types.js",
    "./schemas": "./dist/schemas.js",
    "./validation": "./dist/validation.js",
    "./usage-logging": "./dist/usage-logging/index.js"
  },
  "scripts": {
    "build": "tsc",
    "build:schemas": "npm run build && node scripts/generate-schemas.js",
    "validate": "npm run build && node dist/validation.js",
    "test": "npm run build && node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "test:usage-logging": "npm run build && node --experimental-vm-modules node_modules/jest/bin/jest.js --testPathPatterns=usage-logging",
    "test:all": "npm run test && npm run build:schemas",
    "clean": "rm -rf dist python"
  },
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "@types/uuid": "^10.0.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "uuid": "^13.0.0",
    "zod": "^3.22.0",
    "zod-to-json-schema": "^3.24.6"
  },
  "files": [
    "dist/**/*",
    "README.md"
  ],
  "keywords": [
    "types",
    "schemas",
    "validation",
    "flingoos"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
